# Minimal test configuration
# Use this for quick validation: cargo run -- run-matrix --config configs/test.yaml --out-dir out/test

seeds:
  - "seeds/seed_001.json"

policies:
  - FIFO
  - RANDOM
  - ADVERSARIAL

bounds:
  - "0"
  - "3"
  - "inf"

faults:
  - NONE

schedule_seeds: "0-4"

scheduler_version: "v1.0"
git_commit: ""
